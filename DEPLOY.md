# دیپلوی روی Cloudflare (با D1 موجود)

## ۱. پیدا کردن database_id دیتابیس قبلی
```
npx wrangler d1 list
```
اسم `omidadli01-site-db` رو تو خروجی پیدا کن و `database_id`ش رو کپی کن.

## ۲. تنظیم wrangler.toml
فایل `wrangler.toml` رو باز کن و مقدار `PUT-YOUR-EXISTING-DATABASE-ID-HERE` رو با همون `database_id` جایگزین کن.

## ۳. نصب و لاگین
```
npm install
npx wrangler login
```

## ۴. دیپلوی
```
npm run deploy
```
این دستور اول `vite build` رو اجرا می‌کنه (خروجی `dist/`) و بعد `wrangler deploy` که هم فایل‌های استاتیک هم `worker.js` رو آپلود می‌کنه.

## ۵. تست اتصال به دیتابیس
بعد از دیپلوی، آدرس زیر رو تو مرورگر باز کن (باید یه JSON خالی یا محتوای قبلی برگردونه، نه خطا):
```
https://<your-worker-url>/api/content
```
اگه محتوایی که قبلاً تو D1 ذخیره شده بود اونجا برگشت، یعنی وصله. اگه خالی `{}` بود یعنی جدول تازه ساخته شده (self-healing schema) و هنوز چیزی ذخیره نشده — طبیعیه برای اولین دیپلوی.

## نکات مهم
- **پین پیش‌فرض ادمین:** `1234` (اگه جدول `admin_auth` خالی باشه، خودش این مقدار رو ست می‌کنه). اگه قبلاً پین دیگه‌ای تو دیتابیس قدیمی ذخیره کرده بودی، همون حفظ می‌مونه چون جدول از قبل موجوده و دوباره seed نمی‌شه.
- **localStorage به‌عنوان کش:** سایت اول از localStorage مرورگر لود می‌شه (برای نمایش فوری بدون پرش)، بعد بلافاصله از `/api/content` (یعنی D1) چک می‌کنه و اگه دیتای سرور موجود بود، جایگزینش می‌کنه. یعنی D1 همیشه منبع اصلی حقیقته، نه مرورگر.
- **ذخیره‌سازی:** هر تغییری که در پیشخوان ادمین بدی، هم روی localStorage و هم (با نیم‌ثانیه تأخیر، برای جلوگیری از درخواست‌های زیاد پشت‌سرهم) روی `/api/content` که دیتا رو تو D1 می‌نویسه، ذخیره می‌شه.
- **ری‌ست:** اگه خواستی جدول‌ها رو کامل از نو بسازی، از داخل Cloudflare Dashboard یا با `wrangler d1 execute` جدول‌های `site_content`، `admin_auth`، `admin_sessions` رو drop کن؛ دفعه‌ی بعد که سایت لود بشه خودش دوباره می‌سازتشون.
